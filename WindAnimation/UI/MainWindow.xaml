<Window x:Class="WindAnimation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindAnimation"
        mc:Ignorable="d"
        Title="MainWindow" Width="1080" Height="540">
    <Grid>
    <Image Width="{Binding Width}" Height="{Binding Height}" 
           Stretch="UniformToFill"  VerticalAlignment="Center" HorizontalAlignment="Center" 
           Source="{Binding BlueMarble, Mode=OneTime}">
    </Image>
    <Image Width="{Binding Width}" Height="{Binding Height}" 
           Stretch="UniformToFill"  VerticalAlignment="Center" HorizontalAlignment="Center"
           Source="{Binding ImageSource, Mode=OneWay}">
    </Image>
  </Grid>
</Window>
